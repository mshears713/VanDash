mode: operational
network:
  ssid: "VanDash-Hub"
  password: "dashboard-access"
  fixed_ip: "192.168.4.1"

backend:
  port: 80
  log_level: INFO

camera_rear:
  device_path: "/dev/v4l/by-id/usb-MACROSILICON_USB_Video_20200909-video-index0"
  resolution: [720, 480]
  framerate: 30
  pixel_format: "MJPG"
  simulation: false

obd:
  port: null # null for auto-discovery
  simulation: false
  polling_interval: 0.5

supervision:
  max_retries: 3
  backoff_seconds: 5

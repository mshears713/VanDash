# LAPTOP ONLY - Development and Learning Environment
mode: maintenance

network:
  ssid: "VanDash-Dev"
  password: "dev-access"
  fixed_ip: "127.0.0.1"

backend:
  port: 8000
  log_level: DEBUG

camera_rear:
  device_index: 0
  resolution: [640, 480]
  framerate: 30
  pixel_format: "MJPG"
  simulation: true
  allow_real: true # Set to true to test real hardware on laptop

camera_front:
  device_index: 1
  resolution: [640, 480]
  framerate: 30
  pixel_format: "MJPG"
  simulation: true
  allow_real: true

obd:
  port: null
  simulation: true
  allow_real: true # Set to true to test real adapter on laptop
  polling_interval: 1.0

supervision:
  max_retries: 5
  backoff_seconds: 2.0
